# RK3588 板端运行验证记录

本文档记录已经在 `sfzt@10.10.10.50` 上做过的测试。目标是确认交叉编译产物能在真实 RK3588 板端被 loader 装载、动态库完整，并能消费板端已有 bag。

## 测试对象

验证时关键产物：

```text
/home/user/3588_cross_compile_ws/rk3588_install/lib/libLocUtils.so
/home/user/3588_cross_compile_ws/rk3588_install/lib/libGeographicLib.so.26.0.0
/home/user/3588_cross_compile_ws/rk3588_install/lib/libceres.so.2.1.0
/home/user/3588_cross_compile_ws/rk3588_install/lib/libfmt.so.9.1.1
/home/user/3588_cross_compile_ws/rk3588_build/slam_ui/slam_ui
/home/user/work_ws/loc_map/msf_loc/loc_install/bin/lio_mapping_offline_node
/home/user/work_ws/loc_map/msf_loc/loc_install/bin/web_ui_websocket_node
```

宿主机上已确认这些文件为 ARM aarch64，且 x86 扫描无输出。上述本地测试输出已清理；重新运行 Docker build 可复现。

2026-05-21 复测使用的 Docker 镜像：

```text
registry.cn-hangzhou.aliyuncs.com/bit_robot_image/robot_images:rk3588-cross-20260521
digest: sha256:2fba0e062eb3680e576d25616d5ee8fcd2cffe6103fbea9428b93123cda7d27e
```

## 基础 loader/ldd 测试

板端临时目录使用 `/tmp/rk3588_*`，测试后删除。

检查范围：

- `file` 输出为 ARM aarch64
- `ldd` 没有 `not found`
- `/lib/ld-linux-aarch64.so.1 --verify` 能通过

已检查可执行文件：

```text
slam_ui
lio_matching_node
lio_mapping_node
lio_mapping_offline_node
lio_matching_ui_node
pgo_node
web_ui_websocket_node
```

## Bag 测试

使用板端已有 bag：

```text
/home/sfzt/rosbag/2025-08-14-07-10-44_0.bag
duration: 18.8s
size: 332.7 MB
topics:
  /imu_data
  /rslidar_points
  /ultrasonic_info
  /vehicle_state
```

临时配置核心字段：

```yaml
bag_fold: /home/sfzt/rosbag
lidar_topic: /rslidar_points
imu_topic: /imu_data
vel_topic: /vehicle_state
```

运行形式：

```bash
source /opt/ros/noetic/setup.bash

export LD_LIBRARY_PATH="$TEST_ROOT/install/lib:$TEST_ROOT/loc_install/libs:/usr/local/lib:/opt/ros/noetic/lib:/usr/lib/aarch64-linux-gnu:/lib/aarch64-linux-gnu:${LD_LIBRARY_PATH:-}"

timeout 120s \
  "$TEST_ROOT/loc_install/bin/lio_mapping_offline_node" \
  "$TEST_ROOT/config/mapping/board_existing_bag.yaml"
```

2026-05-21 实测日志出现：

```text
running in /home/sfzt/rosbag/2025-08-14-07-10-44_0.bag
io_utils.cpp:187] finish
```

生成产物：

```text
workspace/map_data/key_frames/1.pcd
workspace/path/laser.txt
workspace/path/gnss.txt
workspace/map_data/trajectory/kf_odom.txt
workspace/map_data/trajectory/keyframe.txt
```

说明：`lio_mapping_offline_node` 已经读完 bag 并生成关键帧/轨迹产物。进程本身没有自然退出，测试时用 `timeout` 收掉；这仍然能证明交叉编译产物可在 RK3588 上启动、链接并消费板端 bag。

## 清理记录

已清理板端临时目录：

```text
/tmp/rk3588_cross_test_1779255852
/tmp/rk3588_bag_test_1779256666
/tmp/rk3588_bag_runtime_1779257354
/tmp/rk3588_sysroot_*
/tmp/rk3588_http_server.log
```

2026-05-21 复查并删除：

```text
/tmp/rk3588_loc_map_test_20260521_032020
/tmp/rk3588_loc_map_test_20260521_032020.tar.gz
```

如需再次清理：

```bash
ssh sfzt@10.10.10.50 \
  'printf "%s\n" "<password>" | sudo -S rm -rf /tmp/rk3588_*'
```

只清理 `/tmp/rk3588_*`，不要清理板端真实 bag 目录：

```text
/home/sfzt/rosbag
```
